# ===========================================
# ComfyUI-omni-llm 依赖配置文件
# ===========================================
# 安装说明：
# 1. 基础安装：pip install -r requirements.txt
#    - 会根据你的 Python 版本和操作系统自动选择合适的 wheel
# 2. 如果自动安装失败或需要特定版本，请手动从以下地址下载：
#    https://github.com/JamePeng/llama-cpp-python/releases
# 3. site-packages 压缩包为适配transformers-5.14.1版本，4.76版本无需安装
# ===========================================

# ===========================================
# 核心基础依赖（必须安装）
# ===========================================
torch
torchaudio
Pillow
requests
numpy
numba
scipy>=1.7.0
diskcache
gguf
packaging
transformers<=4.76
# ===========================================
# llama-cpp-python 依赖（必须安装）
# 使用 JamePeng 构建的带 CUDA/MPS 加速的预编译版本
# 支持 Python 3.10-3.14，自动根据平台和版本选择

# ===========================================
# 音频处理核心依赖
# ===========================================
soundfile
# ===========================================

# ASR 功能依赖（需要使用 Qwen3-ASR 时安装）
# ===========================================
accelerate==1.12.0
librosa>=0.10.0
nagisa>=0.2.11
soynlp==0.0.493
qwen-omni-utils
soundfile
pytz
qwen_asr

# ===========================================
# 安装后验证
# ===========================================
# python -c "import llama_cpp; print('llama-cpp-python 版本:', llama_cpp.__version__)"
# python -c "import numpy; print('numpy 导入成功')"
# python -c "import torch; print('torch 导入成功')"